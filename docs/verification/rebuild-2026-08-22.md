# Rebuild Verification Record — workspace-runtime rebuild

- Date: 2026-08-22
- Branch: `phase-10-vm-stability`
- Baseline before rebuild: `579dbf9` (audit report added at `214604a`)
- Platform: Windows 11 (10.0.26200, AMD64) — rustc/cargo 1.94.1, Node 24.14.1

## Commands and results

| Check | Command | Result |
|---|---|---|
| Unit tests | `cargo test -p lotus-core --lib` | PASS — 12/12 |
| Adversarial integration tests | `cargo test --workspace` | PASS — 25/25 (`supervision`), ~66–90 s wall time |
| Lints | `cargo clippy --workspace` | PASS — 0 warnings |
| Frontend type-check + build | `npm run build` (shell/lotus-shell) | PASS — tsc clean, vite bundle built |
| Desktop crate compile | `cargo check -p lotus-shell` / clippy | PASS — 0 warnings |
| End-to-end CLI lifecycle | `powershell -File scripts\smoke-e2e.ps1` | **SMOKE TEST PASSED** — add→trust→start→HEALTHY→doctor ALL OK→logs captured→events ledgered→checkpoint→restore (new PID, healthy)→stop→OFF |
| Invalid manifest rejection | `lotus add fixtures\invalid-manifests\cyclic-deps` / `unsupported-version` | PASS — exit 1 with precise errors ("cyclic process dependency detected involving: a, b"; "unsupported manifest version = 99") |

## Coverage statement

- Engine behaviors exercised dynamically by the integration suite are listed
  scenario-by-scenario in `docs/testing.md`.
- Windows is the verified platform for every row above; Linux/macOS paths are
  implemented behind the platform adapter but not executed here
  (see `docs/platform-support.md`).
- The Tauri desktop app is verified to compile cleanly and to route every
  command through the same tested engine; its UI layer itself has no automated
  browser tests.

## Residual uncertainty

- Grandchild cleanup asserted only indirectly (via tree-kill paths).
- Ledger/log rotation beyond two generations not stress-tested.
- `npm ci` from a cold cache was not re-run this session.
